# GitLab Migration Configuration
# Copy this file to gitlab-migration.env and fill in your values

# Your GitHub username
GITHUB_USERNAME="your-github-username"

# GitLab Personal Access Token
# Create at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_user, read_repository, write_repository
GITLAB_TOKEN="your-gitlab-token"

# GitHub Personal Access Token (optional but recommended)
# Create at: https://github.com/settings/tokens
# Required scopes: repo
GITHUB_TOKEN="your-github-token"

# GitLab group name (default: limps-suite)
GITLAB_GROUP="limps-suite"

# Repository base directory for local clones (used by deploy-to-repos.sh)
REPO_BASE_DIR="~/code"